export interface ProductInputDTO{
    name: string;
    tags: string[];
}

export interface Product{
    id: number,
    name: string
    
}
export interface Tags{
    id: number,
    tags: string[]
    
}